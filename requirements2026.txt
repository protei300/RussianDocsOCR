# Переходная ссылка: с версии 4.6.0 проверенный набор зависимостей лежит в
# requirements.txt. Этот файл оставлен, чтобы не сломать скрипты и сборки,
# которые ставят по старому имени, и будет удалён в одной из следующих версий.
# Ставьте:  pip install -r requirements.txt
-r requirements.txt
