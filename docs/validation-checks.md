# Валидационные проверки полей документов

Рабочий список проверок, которые можно реализовать поверх результатов OCR
(и/или использовать в генераторе синтетики как санити-чек форматов).
Статусы: `реализовано` (уже есть в пайплайне), `todo` (кандидат на реализацию),
`идея` (нужно уточнить правила перед реализацией).

Типы проверок:
- **формат** — регулярка/маска на одно поле;
- **чексумма** — арифметическая контрольная сумма внутри поля;
- **кросс-поле** — согласованность нескольких полей одного документа;
- **словарь** — сверка со справочником.

Жёсткость: **жёсткая** проверка обязана сходиться у корректно распознанного
документа (несхождение = почти наверняка ошибка OCR); **мягкая** — типичное,
но не гарантированное правило (несхождение = повод понизить confidence, не более).

> Назначение — контроль качества распознавания, не проверка подлинности
> документа. Все правила ниже — из открытых источников (форматы бланков,
> ГОСТы, ICAO 9303).
>
> Для генератора синтетики договорённость обратная: контрольные суммы
> **намеренно не делаем валидными**, чтобы сгенерированные номера были
> математически невозможными (см. обсуждение в progress-log).

---

## Общие (все типы документов)

| ID | Проверка | Тип | Жёсткость | Статус |
|----|----------|-----|-----------|--------|
| GEN-1 | Все даты — существующие календарные даты (31.02 невозможно) | формат | жёсткая | todo |
| GEN-2 | `Birth_date` < `Issue_date`; обе не в будущем | кросс-поле | жёсткая | todo |
| GEN-3 | Возраст на дату выдачи в правдоподобном диапазоне (документозависимо, см. ниже) | кросс-поле | жёсткая | todo |
| GEN-4 | Допустимый алфавит поля (кириллица/латиница/цифры) | формат | жёсткая | реализовано — маскирование в CTC-декодере (`config/ocr_alphabets.json`) |

---

## SNILS

Формат номера: `XXX-XXX-XXX YY` — 9 цифр номера + 2 цифры контрольного числа.

| ID | Проверка | Тип | Жёсткость | Статус |
|----|----------|-----|-----------|--------|
| SNILS-1 | Маска `\d{3}-\d{3}-\d{3} \d{2}` (пробел иногда распознаётся как второй дефис — нормализовать до сверки) | формат | жёсткая | todo |
| SNILS-2 | Контрольное число (алгоритм ниже) | чексумма | жёсткая | todo |
| SNILS-3 | Дата регистрации в формате «DD месяца YYYY года» (месяц родительным падежом, словарь из 12 значений) | формат/словарь | жёсткая | todo |

**Алгоритм SNILS-2.** Каждая из 9 цифр номера умножается на вес: первая слева
— на 9, вторая — на 8, …, девятая — на 1; произведения суммируются.
Далее:
- сумма < 100 → контрольное число = сумма;
- сумма ∈ {100, 101} → контрольное число `00`;
- сумма > 101 → взять `сумма mod 101` и применить те же правила.

Нюанс: проверка формально применяется только к номерам > `001-001-998`
(меньшие не проверяются).

Пример: `112-233-445` → 1·9+1·8+2·7+2·6+3·5+3·4+4·3+4·2+5·1 = 95 → `112-233-445 95`.

---

## INTPASSPORT (внутренний паспорт РФ)

Серия/номер: `СС СС НННННН` — 4 цифры серии + 6 цифр номера. Контрольной
суммы у номера **нет**.

| ID | Проверка | Тип | Жёсткость | Статус |
|----|----------|-----|-----------|--------|
| PASS-1 | Маска серии/номера: `\d{2} \d{2} \d{6}` | формат | жёсткая | todo |
| PASS-2 | Первые 2 цифры серии — код региона по ОКАТО (существующий регион, справочник ~90 значений) | словарь | жёсткая | todo |
| PASS-3 | Вторая пара цифр серии — год выпуска бланка; обычно равен году выдачи ±2–3 года (бланки печатают заранее/расходуют с запозданием) | кросс-поле | **мягкая** | todo |
| PASS-4 | Код подразделения: маска `\d{3}-\d{3}`; первые 2 цифры = код региона (обычно совпадает с PASS-2) | формат/кросс-поле | мягкая | todo |
| PASS-5 | Третья цифра кода подразделения — уровень подразделения (0 — УФМС, 1 — паспортный стол ГУВД/МВД региона, 2 — рай-/горотдел, 3 — тер. пункт) | формат | мягкая | todo |
| PASS-6 | Код подразделения ↔ название органа выдачи | словарь | мягкая | **реализовано** — `FMSSearcher` (`ctc/ctc_functions.py`, вызывается из `Pipeline._fix_fms`) |
| PASS-7 | Возраст на дату выдачи ≥ 14 лет; типичные выдачи в 14 / 20 / 45 лет (замена по возрасту) — вне этих окон допустимо (утеря, смена фамилии), поэтому только мягкий сигнал | кросс-поле | жёсткая (≥14) / мягкая (окна) | todo |
| PASS-8 | Пол ∈ {`МУЖ.`, `ЖЕН.`} (с вариациями регистра/точки) | формат | жёсткая | todo |
| PASS-9 | Согласование пола с окончанием отчества (`-вич`/`-вна` и др.) | кросс-поле | мягкая | идея |

---

## INTPASSPORTADDR (страница регистрации)

| ID | Проверка | Тип | Жёсткость | Статус |
|----|----------|-----|-----------|--------|
| ADDR-1 | Дата регистрации — календарная, не в будущем | формат | жёсткая | todo |
| ADDR-2 | Компоненты адреса против справочника ФИАС/КЛАДР (регион, город, улица) | словарь | мягкая | идея — справочник большой, нужен план по размеру/обновлению |

---

## DL (водительское удостоверение)

Серия/номер: 10 цифр — `СССС ННИНННН` (4 серии + 6 номера). Публичной
контрольной суммы **нет**.

| ID | Проверка | Тип | Жёсткость | Статус |
|----|----------|-----|-----------|--------|
| DL-1 | Маска серии/номера: 10 цифр (в серии образца до ~2011 могли быть кириллические буквы — учитывать для эры 2011−) | формат | жёсткая | todo |
| DL-2 | Поле 4b (окончание срока) = поле 4a (дата выдачи) + ровно 10 лет — для образца 2011+ | кросс-поле | жёсткая (2011+) | todo |
| DL-3 | Первые 2 цифры серии — код региона; обычно совпадает с регионом подразделения ГИБДД (поле 4c) | кросс-поле | мягкая | todo |
| DL-4 | Возраст на дату выдачи ≥ 16 лет (категория M/A1), ≥ 18 для B/C | кросс-поле | жёсткая | todo |
| DL-5 | Кросс-сверка кириллических и латинских дублей ФИО (транслитерация) | кросс-поле | мягкая — стандартов транслитерации несколько (ГОСТ/ICAO, менялись по годам), сверять нестрого, по расстоянию | todo |
| DL-6 | Поле категорий — подмножество {A, A1, B, B1, C, C1, D, D1, BE, CE, C1E, DE, D1E, M, Tm, Tb} | словарь | жёсткая | todo |
| DL-7 | Поле 5 (номер ВУ) на лицевой стороне = номеру на обороте (если распознаются оба) | кросс-поле | жёсткая | идея |

---

## EXTPASSPORT (заграничный паспорт)

Серия/номер: `СС ННННННН` — 2 цифры серии + 7 цифр номера.

| ID | Проверка | Тип | Жёсткость | Статус |
|----|----------|-----|-----------|--------|
| EXT-1 | Маска серии/номера: `\d{2} \d{7}` | формат | жёсткая | todo |
| EXT-2 | Серия соответствует типу бланка: 70–74 — биометрические (10 лет), 60-е — небиометрические (5 лет), 50-е и ниже — старые серии | словарь | мягкая | todo |
| EXT-3 | Срок действия: `Expiration_date − Issue_date` = 10 лет (биометрический) или 5 лет (обычный); согласовать с EXT-2 | кросс-поле | жёсткая | todo |
| EXT-4 | MRZ: контрольные цифры ICAO 9303 (алгоритм ниже) | чексумма | жёсткая | идея — MRZ сейчас не входит в состав распознаваемых полей; добавить поле → получить самый сильный чекер этого документа |
| EXT-5 | Кросс-сверка MRZ ↔ визуальная зона: номер, даты, фамилия/имя в транслитерации | кросс-поле | жёсткая | идея (зависит от EXT-4) |
| EXT-6 | Пол: `М/M`, `Ж/F` согласованы между рус/лат полями | кросс-поле | жёсткая | todo |

**Алгоритм EXT-4 (ICAO 9303, TD3 — 2 строки по 44 символа).**
Контрольная цифра считается по строке с весами, циклически 7-3-1:
цифры имеют своё значение, буквы `A..Z` → 10..35, заполнитель `<` → 0.
Сумма произведений mod 10 = контрольная цифра. В строке 2 проверяются:
номер документа (позиции 1–9, КЦ в 10), дата рождения (14–19, КЦ в 20),
дата окончания (22–27, КЦ в 28), personal number (29–42, КЦ в 43; у
российских паспортов там дата рождения повторно + серия, но допустим и
пустой `<`-заполненный), и финальная составная КЦ (позиция 44) — по
конкатенации перечисленных полей вместе с их КЦ.

---

## Кандидаты для будущих типов документов

Пригодится, когда начнём добавлять новые типы (по плану генерации синтетики):

- **ИНН**: 10 цифр (юрлицо, 1 КЦ) / 12 цифр (физлицо, 2 КЦ) — веса опубликованы,
  жёсткая чексумма.
- **ОГРН/ОГРНИП**: 13/15 цифр, последняя цифра = остаток от деления
  первых 12/14 на 11/13 (mod 10) — жёсткая чексумма.
- **Свидетельство о рождении**: серия `римское число-ЛЛ` (две кириллические
  буквы) + 6 цифр номера — формат.
- **Полис ОМС**: 16 цифр, последняя — контрольная по алгоритму Луна — жёсткая
  чексумма.
- **Банковская карта** (если вдруг): алгоритм Луна.
