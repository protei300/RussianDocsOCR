"""RussianDocs recognition service.

A reference implementation of how to wrap the ``document_processing`` library
in a web service. The library itself is never modified — everything here is a
consumer of its public API.
"""

__version__ = "0.1.0"
